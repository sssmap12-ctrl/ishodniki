{
  "name": "hamari-chat-novel",
  "version": "1.0.0",
  "description": "2D Visual Novel Chat Simulator with Hamari",
  "main": "electron.js",
  "scripts": {
    "start-pc": "electron .",
    "build": "node build.js",
    "android-init": "npx cap init \"Чат с Хамари\" \"com.hamari.chat\" --web-dir=www",
    "android-add": "npx cap add android",
    "android-sync": "node build.js && powershell -ExecutionPolicy Bypass -File make_icons.ps1 && npx cap sync",
    "android-open": "npx cap open android",
    "android-build": "node build.js && powershell -ExecutionPolicy Bypass -File make_icons.ps1 && npx cap sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "electron": "^42.3.0"
  }
}
